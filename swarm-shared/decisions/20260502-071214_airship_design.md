# airship / design

### Highest-Value Incremental Improvement
The highest-value incremental improvement that can be shipped in <2h is to implement the HF CDN Bypass pattern to avoid rate-limit blocks during dataset training. This involves pre-listing file paths once, embedding them in the training script, and using the CDN to download dataset files without authorization headers.

### Implementation Plan
1. **Identify the dataset repository**: Determine the repository containing the dataset to be used for training.
2. **List file paths**: Use the `list_repo_tree` API to list the file paths in the repository. This should be done only once to avoid rate-limit issues.
3. **Save file paths to JSON**: Save the listed file paths to a JSON file.
4. **Embed JSON in training script**: Modify the training script to read the file paths from the JSON file and use them to download the dataset files from the CDN.
5. **Use CDN for dataset downloads**: Update the training script to download dataset files from the CDN using the `https://huggingface.co/datasets/{repo}/resolve/main/{path}` URL pattern, without authorization headers.

### Code Snippets
```bash
# List file paths using list_repo_tree API
file_paths=$(curl -X GET \
  https://huggingface.co/api/v1/datasets/{repo}/list_repo_tree \
  -H 'Authorization: Bearer {token}' \
  -H 'Content-Type: application/json')

# Save file paths to JSON file
echo "$file_paths" > file_paths.json
```

```python
# Embed JSON in training script
import json

with open('file_paths.json') as f:
    file_paths = json.load(f)

# Use CDN for dataset downloads
for file_path in file_paths:
    file_url = f"https://huggingface.co/datasets/{repo}/resolve/main/{file_path}"
    # Download file from CDN without authorization headers
    response = requests.get(file_url)
    # Process downloaded file
```

### Benefits
This implementation plan avoids rate-limit blocks during dataset training by using the HF CDN Bypass pattern. It reduces the number of API calls and allows for faster dataset downloads, resulting in improved training efficiency.
