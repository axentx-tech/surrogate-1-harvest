# vanguard / design

### 1. **Diagnosis**
* The Vanguard project lacks a comprehensive solution to handle HF API rate limits, which can block dataset training and hinder the project's progress.
* The current implementation does not utilize the HF CDN bypass strategy, which can download public dataset files without being limited by the HF API rate limits.
* The project's design does not incorporate a mechanism to pre-list file paths once and embed them in the training script, which can reduce the number of API calls and avoid rate limits.
* The existing implementation may not be optimized for performance, leading to potential bottlenecks and inefficiencies in the training pipeline.

### 2. **Proposed change**
The proposed change involves modifying the `train.py` script to utilize the HF CDN bypass strategy and pre-list file paths once. The scope of the change includes:
* Modifying the `train.py` script to download public dataset files from the HF CDN using the `https://huggingface.co/datasets/{repo}/resolve/main/{path}` URL pattern.
* Adding a mechanism to pre-list file paths once and embed them in the training script using a JSON file.
* Updating the training pipeline to use the pre-listed file paths and avoid making unnecessary API calls.

### 3. **Implementation**
The implementation involves the following steps:
```python
# Step 1: Pre-list file paths once and save to a JSON file
import json
import requests

def pre_list_file_paths(repo, path):
    url = f"https://huggingface.co/datasets/{repo}/resolve/main/{path}"
    response = requests.get(url)
    file_paths = response.json()
    with open("file_paths.json", "w") as f:
        json.dump(file_paths, f)

# Step 2: Modify the train.py script to use the pre-listed file paths
import json

def train():
    with open("file_paths.json", "r") as f:
        file_paths = json.load(f)
    # Use the pre-listed file paths to download dataset files from the HF CDN
    for file_path in file_paths:
        url = f"https://huggingface.co/datasets/{repo}/resolve/main/{file_path}"
        response = requests.get(url)
        # Process the dataset file
        pass

# Step 3: Update the training pipeline to use the modified train.py script
import subprocess

def run_training_pipeline():
    subprocess.run(["python", "train.py"])
```
### 4. **Verification**
To verify that the proposed change works, the following steps can be taken:
* Run the modified `train.py` script and verify that it downloads the dataset files from the HF CDN without making unnecessary API calls.
* Check the `file_paths.json` file to ensure that it contains the pre-listed file paths.
* Monitor the training pipeline's performance and verify that it is optimized and efficient.
* Test the training pipeline with different datasets and repositories to ensure that it works as expected.
