# vanguard / ops

### 1. **Diagnosis**
* The Vanguard project lacks a comprehensive solution to handle HF API rate limits, which can block dataset training and hinder the project's progress.
* The current implementation does not utilize the HF CDN bypass strategy, which can download public dataset files without being limited by the HF API rate limits.
* The project's ops focus requires a reliable and efficient way to handle dataset training, and the current implementation falls short of this requirement.

### 2. **Proposed change**
The proposed change is to implement the HF CDN bypass strategy in the `granite-business-research.sh` script, which is responsible for running market analysis and querying top hub and related docs for contextual insights. Specifically, the change will be made in the `download_dataset` function, which is currently using the HF API to download dataset files.

### 3. **Implementation**
To implement the HF CDN bypass strategy, we will modify the `download_dataset` function to use the `https://huggingface.co/datasets/{repo}/resolve/main/{path}` URL to download public dataset files. This will bypass the HF API rate limits and allow for more efficient dataset training.

Here is an example of the modified `download_dataset` function:
```bash
download_dataset() {
  repo=$1
  path=$2
  url="https://huggingface.co/datasets/${repo}/resolve/main/${path}"
  curl -o "${path}" "${url}"
}
```
We will also add a check to ensure that the dataset file is downloaded successfully before proceeding with the training process.

### 4. **Verification**
To verify that the proposed change works, we can run the `granite-business-research.sh` script and check that the dataset files are downloaded successfully using the HF CDN bypass strategy. We can also monitor the project's logs and metrics to ensure that the dataset training process is no longer blocked by HF API rate limits.

Additionally, we can test the modified `download_dataset` function by running it independently and verifying that it downloads the dataset files correctly. We can use tools like `curl` and `wget` to test the download process and ensure that the files are downloaded successfully.
