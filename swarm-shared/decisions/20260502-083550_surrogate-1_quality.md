# surrogate-1 / quality

### Diagnosis
* The project lacks a robust implementation for handling Hugging Face API rate limits, which can block dataset training.
* The existing implementation may not be reusing existing Lightning Studio instances efficiently, leading to wasted resources.
* The project does not have a mechanism to bypass the Hugging Face API rate limit for dataset training, which can cause significant delays.
* The current implementation of the training pipeline may not be optimized for performance, leading to slower training times.
* The project does not have a clear strategy for handling errors and exceptions, which can cause instability and downtime.

### Proposed change
The proposed change is to implement a mechanism to bypass the Hugging Face API rate limit for dataset training by using the HF CDN to download dataset files directly. This can be achieved by modifying the `train.py` script to use the `https://huggingface.co/datasets/{repo}/resolve/main/{path}` URL to download dataset files instead of using the Hugging Face API.

### Implementation
To implement this change, we need to modify the `train.py` script as follows:
```python
import json
import os

# Load the list of dataset files from the JSON file
with open('dataset_files.json') as f:
    dataset_files = json.load(f)

# Define the HF CDN URL template
cdn_url_template = 'https://huggingface.co/datasets/{repo}/resolve/main/{path}'

# Download the dataset files using the HF CDN
for file in dataset_files:
    repo = file['repo']
    path = file['path']
    cdn_url = cdn_url_template.format(repo=repo, path=path)
    # Download the file using the CDN URL
    os.system(f'wget {cdn_url}')
```
We also need to create a JSON file `dataset_files.json` that contains the list of dataset files to download. This file can be generated by running a script that uses the Hugging Face API to list the files in the dataset repository.

### Verification
To verify that this change works, we can run the `train.py` script and check that the dataset files are downloaded correctly using the HF CDN. We can also check the Hugging Face API logs to ensure that the rate limit is not being exceeded.

We can add some logging statements to the `train.py` script to verify that the files are being downloaded correctly:
```python
import logging

# ...

# Download the dataset files using the HF CDN
for file in dataset_files:
    repo = file['repo']
    path = file['path']
    cdn_url = cdn_url_template.format(repo=repo, path=path)
    # Download the file using the CDN URL
    logging.info(f'Downloading {path} from {cdn_url}')
    os.system(f'wget {cdn_url}')
    logging.info(f'Download complete: {path}')
```
By checking the logs, we can verify that the files are being downloaded correctly using the HF CDN.
